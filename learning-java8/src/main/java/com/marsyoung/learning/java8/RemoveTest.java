package com.marsyoung.learning.java8;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Set;
//import java.util.stream.Collectors;

/**
 * Created by mazhiyu on 2017/6/20.
 */
public class RemoveTest {

    public static void main(String[] args) {
//        Set<Key> notCached = Arrays.asList(ids).parallelStream().collect(Collectors.toSet());
//        Set<String> keys = notCached.parallelStream().map(one -> buildCachePrefix(one)).collect(Collectors.toSet());
//        List<Entity> resultFromCache = redisCache.mget(keys);
//        if (resultFromCache != null && resultFromCache.size() != 0) {
//            resultFromCache.parallelStream().forEach(n -> {
//                if (n != null) {
//                    cacheResult.add(n);
//                    notCached.remove(n.getCacheKey());
//                }
//            });
//        }
//        return notCached;

    }
}
